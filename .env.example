# RabbitMQ credentials
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=changeme

# MySQL datasource
DATASOURCE_URL=jdbc:mysql://localhost:3306/edtech_course_service?useSSL=false
DATASOURCE_USERNAME=root
DATASOURCE_PASSWORD=changeme

# Placeholder for service-to-service auth token forwarded to Feign calls.
# Replace with a real token-issuing mechanism (e.g. OAuth2 client-credentials) in production.
USER_SERVICE_AUTH_TOKEN=
